name: ChromeXt Android CI (Self-Hosted, China-Friendly)

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
    # 0️⃣ 系统代理设置（Clash 代理）
    - name: Set system proxy
      run: |
        echo "http_proxy=http://192.168.32.222:7897" >> $GITHUB_ENV
        echo "https_proxy=http://192.168.32.222:7897" >> $GITHUB_ENV
        export http_proxy=http://192.168.32.222:7897
        export https_proxy=http://192.168.32.222:7897

    # 1️⃣ 设置 Gradle 代理
    - name: Set Gradle proxy
      run: |
        echo "org.gradle.jvmargs=-Dhttp.proxyHost=192.168.32.222 -Dhttp.proxyPort=7897 -Dhttps.proxyHost=192.168.32.222 -Dhttps.proxyPort=7897" >> gradle.properties
        export GRADLE_OPTS="-Dhttp.proxyHost=192.168.32.222 -Dhttp.proxyPort=7897 -Dhttps.proxyHost=192.168.32.222 -Dhttps.proxyPort=7897"

    # 2️⃣ 获取源码
    - uses: actions/checkout@v3

    # 3️⃣ 确认 JDK 已安装
    - name: Use system JDK
      run: java -version

    # 4️⃣ 构建 debug APK
    - name: Build debug APK
      run: ./gradlew app:assembleDebug

    # 5️⃣ 构建 release APK
    - name: Build release APK
      if: ${{ (github.event_name != 'pull_request' && github.ref == 'refs/heads/master') || github.ref_type == 'tag' }}
      run: ./gradlew app:assembleRelease

    # 6️⃣ 重命名 APK
    - name: Rename debug APK
      run: mv app/build/outputs/apk/debug/app-debug.apk app/build/ChromeXt_debug.apk

    - name: Rename release APK
      if: ${{ (github.event_name != 'pull_request' && github.ref == 'refs/heads/master') || github.ref_type == 'tag' }}
      run: mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/ChromeXt.apk

    # 7️⃣ 签名 release APK
    - name: Sign release APK
      if: ${{ (github.event_name != 'pull_request' && github.ref == 'refs/heads/master') || github.ref_type == 'tag' }}
      uses: noriban/sign-android-release@master
      id: sign_app
      with:
        releaseDirectory: app/build/outputs/apk/release
        signingKeyBase64: ${{ secrets.SIGNING_KEY }}
        alias: ${{ secrets.ALIAS }}
        keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
        keyPassword: ${{ secrets.KEY_PASSWORD }}

    # 8️⃣ 上传 debug APK
    - name: Upload debug APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: ChromeXt_debug.apk
        path: app/build/ChromeXt_debug.apk

    # 9️⃣ 上传签名 release APK
    - name: Upload signed release APK
      if: ${{ (github.event_name != 'pull_request' && github.ref == 'refs/heads/master') || github.ref_type == 'tag' }}
      uses: actions/upload-artifact@v4
      with:
        name: ChromeXt_signed.apk
        path: ${{ steps.sign_app.outputs.signedReleaseFile }}

    # 🔟 可选：创建 GitHub Release
    - name: Create GitHub Release
      if: ${{ (github.event_name != 'pull_request' && github.ref == 'refs/heads/master') || github.ref_type == 'tag' }}
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.sha }}
        name: ChromeXt Release ${{ github.sha }}
        files: |
          app/build/ChromeXt_debug.apk
          ${{ steps.sign_app.outputs.signedReleaseFile }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
